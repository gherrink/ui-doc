# Revisions that only moved code around. Skip them in `git blame`.
#
# Configure this once per clone:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub reads this file automatically and needs no setup.
#
# Note: master is rebase-merged, so a commit's final SHA is only known after
# the merge. Any entry added here has to be confirmed against master
# afterwards - `git merge-base --is-ancestor <sha> master` will say.

# style: adopt oxfmt and reformat
57fdde8cfee94dd5f9f1e002d3a60d02d94f3547
