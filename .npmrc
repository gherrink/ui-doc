# Publishing in CI uses GitHub OIDC (npm Trusted Publisher).
# For local publishing, authenticate via ~/.npmrc.

# pnpm options
enable-pre-post-scripts = true
shamefully-hoist = true
strict-peer-dependencies = false
